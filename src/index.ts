#!/usr/bin/env node

/**
 * Demo Creator - Main Entry Point
 *
 * This application creates demo accounts for the Clusterix HR system.
 * It runs as a web UI server on port 3000.
 */

// Import and start the UI server directly
import './ui-server';
